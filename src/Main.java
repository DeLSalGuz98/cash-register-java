import gui.Gui;
import gui.Views;
import product.*;
import storage.Storage;

public class Main {
    public static void main(String[] args) {
        Gui newGui = new Gui();
        newGui.StartGui();
    }
}